// Complete the code  


function computeValue(a, b) {
    //Write function logic as per the task on the left
    console.log(a*a + 2*a*b + b*b)
    console.log(a+b)
}
 
const A = 3; 
const B = 5; 
computeValue(A, B);
 
 